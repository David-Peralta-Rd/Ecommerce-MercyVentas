[project]
name = "mercyventas"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "David-Peralta-Rd", email = "crdavidperalta@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "django>=6.1.1",
]

[project.scripts]
core = "mercyventas.cli:run_core"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
